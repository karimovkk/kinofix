export interface MovieInfo {
  title: string;
  year: string;
  summary: string;
}

export type LangType = 'uz' | 'ru' | 'en';

export interface TranslationType {
  title: string;
  home: string;
  help: string;
  heroTitle: string;
  heroSubtitle: string;
  description: string;
  uploadTitle: string;
  uploadDesc: string;
  identifyBtn: string;
  identifying: string;
  error: string;
  fileError: string;
  found: string;
  year: string;
  about: string;
  googleBtn: string;
  waiting: string;
  waitingDesc: string;
  footer: string;
}
